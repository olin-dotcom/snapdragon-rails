<%= form_for(@burst) do |f| %>
  <% if @burst.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@burst.errors.count, "error") %> prohibited this burst from being saved:</h2>

      <ul>
      <% @burst.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datetime %><br />
    <%= f.datetime_select :datetime %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.text_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
